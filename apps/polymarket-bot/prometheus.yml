# Prometheus configuration
# Save this as prometheus.yml in the project root

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'polymarket-bot'
    static_configs:
      - targets: ['polymarket-bot:9090']
